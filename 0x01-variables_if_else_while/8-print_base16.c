#include <stdio.h>
/* more headers goes there */
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char gg;

	char lol;

	for (gg = 48; gg <= 57; gg++)
		putchar(gg);
	for (lol = 97 ; lol <= 102; lol++)
		putchar(lol);
	putchar('\n');
	return (0);
}
