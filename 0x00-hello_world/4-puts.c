i#!/bin/bash
nclude <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
pust (tes);
    return (0);
}
int main(void)
{
pust ("/"Programming is like building a multilingual puzzle);
    return (0);
}
