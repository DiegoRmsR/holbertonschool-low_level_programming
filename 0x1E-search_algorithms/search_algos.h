#ifndef SHEARCH_ALGOS_H
#define SHEARCH_ALGOS_H

/**Libraries*/
#include <stdio.h>
#include <stdlib.h>

/**Prototypes*/
int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t size, int value);
void print(int *array, unsigned int start, unsigned int end)
#endif
